import { EditorPlugin } from '../../types';
export declare const findReplacePlugin: (props: {
    takeFullWidth: boolean;
    twoLineEditorToolbar: boolean;
}) => EditorPlugin;
export default findReplacePlugin;
