import { SafePlugin } from '@atlaskit/editor-common/safe-plugin';
import { PMPluginFactoryParams } from '../../../types';
export declare const createPlugin: ({ dispatch, providerFactory, }: PMPluginFactoryParams) => SafePlugin<any, any>;
